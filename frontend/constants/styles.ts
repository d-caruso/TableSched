/** Shared Tamagui pressStyle — use on any Stack/XStack/YStack with onPress. */
export const PRESS_STYLE = { opacity: 0.7 } as const;

/** flex: 1 for components that don't accept Tamagui props (e.g. GestureHandlerRootView). */
export const FLEX_ONE = { flex: 1 } as const;
